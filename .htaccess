# PHP limits are configured in `.user.ini` for Hostinger/shared hosting.
# Keep this file minimal to avoid `500 Internal Server Error` from unsupported
# `php_value` directives under FastCGI/PHP-FPM.
